# ICL Experts MCP Server Dependencies

# Core MCP server framework
fastmcp>=2.0.0

# Optional fuzzy matching capabilities (for enhanced expert routing)
# Note: thefuzz was referenced in error messages, adding for potential future use
thefuzz>=0.19.0

# Core Python libraries (typically included in Python standard library)
# Listed here for explicit dependency documentation
pathlib2>=2.3.0; python_version < '3.4'  # Pathlib backport for older Python

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
# mypy>=1.0.0